import { RequestManager, Poller } from './RequestManager';
export declare type SyncingCallback = (error: Error | null, data) => void;
export declare class IsSyncing extends Poller {
    pollId: string;
    callbacks: SyncingCallback[];
    lastSyncState: any;
    constructor(requestManager: RequestManager);
    addCallback(callback: SyncingCallback): void;
    stopWatching(): Promise<void>;
    /**
     * Adds the callback and sets up the methods, to iterate over the results.
     *
     * @method pollSyncing
     */
    private pollSyncing();
}
